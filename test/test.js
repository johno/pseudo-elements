var assert = require('assert');
var pseudoElements = require('..');

describe('pseudo-elements', function() {

  it('should do something awesome', function() {
    assert.equal(pseudoElements(), true);
  });
});
